spring:
  application:
    name: IF-2050
  datasource:
    url: jdbc:postgresql://localhost:5433/DONEATEdb
    username: admin
    password: password123 # Hardcoded. No brackets, no variables.

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect